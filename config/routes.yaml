noteCreate:
    path: /note
    methods: [ post ]
    controller: App\Controller\NoteCreatorController::handleRequest

noteGet:
    path: /note/{noteId}
    methods: [ get ]
    controller: App\Controller\NoteGetterController::handleRequest
    requirements:
        noteId: '\d+'
